# Codebase Audit — 2026-04-07

Full health audit performed by 5 parallel agents. Findings below drive the refactoring roadmap.

## Status of Fixes

### Done This Session
- [x] Centralize PaymentMethod enum → `core/enums.py`
- [x] Add Tech.routes bidirectional relationship
- [x] DeepBlue: add `require_feature("deepblue")` gate + replace 13 manual role checks with `require_roles()`
- [x] Remove 16 deprecated pool/equipment fields from PropertyCreate/PropertyUpdate schemas
- [x] BOW→WF rename across all active code and docs
- [x] Fix `body_of_water_id` bug in water_feature_service.py SatelliteAnalysis creation
- [x] Fix `OrganizationUser` import path in agent_thread_service.py

### Remaining — Code Health

#### Backend Services (oversized / mixed responsibilities)

| File | Lines | Problem | Fix | Prep for Agent |
|------|-------|---------|-----|----------------|
| `deepblue/tools.py` | 1,620 | 29 tool executors in one file | Split by domain (chemistry, equipment, customer, route, etc.) | Each domain file → tool set for dedicated agent |
| `inspection/service.py` | 1,210 | Near-identical to emd/service.py | Extract base class, parameterize model classes | Pool Scout agent foundation |
| `emd/service.py` | 1,190 | Duplicate of inspection/ | Merge into inspection/ with base class | Pool Scout agent foundation |
| `agent_thread_service.py` | 1,149 | Thread queries + email composition + job creation + estimates + drafting | Split: ThreadService, EmailDraftService, ThreadToJobConverter | Email agent separation |
| `agent_action_service.py` | 855 | Jobs + tasks + estimates tangled | Extract EstimateService | Estimate agent foundation |
| `profitability_service.py` | 772 | 15 proxy methods + 4 real methods | Remove unnecessary delegation layer | Profitability Analyst agent |
| `visit_experience_service.py` | 644 | Mixed concerns (photos, charges, checklists) | Extract VisitPhotoService, VisitChargeService | Service Narrator agent |
| `chemical_cost_service.py` | 614 | Volume estimation + cost calc tangled | Split VolEstimation from CostCalc | Chemistry Advisor agent |

#### Backend Routers (business logic that belongs in services)

| File | Lines | Problem | Fix |
|------|-------|---------|-----|
| `deepblue.py` | 1,145 | Streaming logic, eval runner, token costs inline | Extract DeepBlueAnalyticsService, EvalService |
| `invoices.py` | 681 | Estimate approval workflow, email sending, PDF gen in route bodies | Extract EstimateApprovalService |
| `admin_threads.py` | 661 | Thread approval/revision with inline DB queries | Move queries to AgentThreadService |
| `admin_messages.py` | 623 | AI drafting, follow-up logic, action eval inline | Extract EmailDraftingService |
| `inspection.py` | 608 | Facility access, tier checking, dashboard aggregation | Extract InspectionAccessService |
| `permissions.py` | 597 | Role CRUD with inline permission resolution | Use PermissionService consistently |
| `admin_actions.py` | 521 | Estimate sending workflow with email logic | Extract ActionWorkflowService |
| `public.py` | 311 | Approval workflow, job creation, notifications | Extract PublicApprovalService |

#### Frontend (oversized pages)

| Page | Lines | Problem | Fix |
|------|-------|---------|-----|
| `/cases/[id]` | 988 | Timeline, jobs, invoices, emails all inline | Extract TimelineItem, JobSummary, InvoiceSummary components |
| `/invoices/[id]` | 924 | Edit form, line items, approval workflow | Extract InvoiceLineItemEditor, ApprovalFlow |
| `/dev` | 917 | Agent metrics, eval results, scraper UI | Extract per-section components (dev-only, lower priority) |
| `/invoices` | 829 | List + create + chart + approval | Extract CreateInvoiceDialog, MonthlyChart |
| `/team` | 717 | Table + edit + role assignment | Extract TeamMemberDialog, RoleAssignment |
| `/parts` | 678 | Equipment + parts + vendor | Extract EquipmentList, PartsSearch, VendorPanel |
| `/map` | 673 | 70+ useState declarations | Use useReducer or useMapState() hook |
| `/customers` | 587 | Client list with sections | Extract CustomerRow, SectionHeader |
| `/settings` | 551 | Multiple setting sections | Extract per-section components |
| `/profitability` | 522 | Portfolio view, month selection | Extract ProfitabilityTable, MonthSelector |
| `/jobs` | 522 | Job list, filter, action detail | Extract JobFilters, ActionDetailSheet |
| `/deepblue` | 515 | Chat + tool cards + prompt management | Extract ToolCardRenderer, PromptManager |
| `/invoices/new` | 511 | Invoice creation form | Extract LineItemEditor |
| `/inbox` | 454 | Thread list, filters, pagination | Extract ThreadRow, InboxFilters |
| `/profitability/[customerId]` | 453 | Customer profit breakdown | Extract CostWaterfall, DifficultySliders |

#### Data Model Issues

| Issue | Status |
|-------|--------|
| Duplicate PaymentMethod enum | **FIXED** — centralized in core/enums.py |
| Missing Tech.routes relationship | **FIXED** — bidirectional back_populates |
| Deprecated Property columns writable | **FIXED** — removed from schemas |
| OrgRole name collision (enum vs model) | Documented, not critical |
| Duplicate `map/` and `maps/` component dirs | `maps/` is legacy (routes page), `map/` is current |
| `tw-animate-css` dependency unused | Remove from package.json |
| `radix-ui` root package unused | Remove from package.json |
| API `upload()` missing 401 retry | Add retry logic to match other methods |

#### Patterns to Standardize

- 30+ routers contain direct `db.execute()` calls (should delegate to services)
- Duplicated table/search/filter/pagination pattern across 6+ frontend pages → extract `useListPage()` hook
- Late imports in services to avoid circular deps → consider DI pattern
- Inconsistent presenter usage (some routers manually build responses)

## Refactoring Priority (aligned with agent roadmap)

| Priority | What | Why | Prep for |
|----------|------|-----|----------|
| 1 | ~~Merge inspection/emd services~~ | ~~-2,400 lines, base class + adapter pattern~~ | **DONE** |
| 1b | Site Discovery Agent (dev tool) | Auto-generate scraper adapters from county URLs | Scalable Pool Scout expansion |
| 2 | ~~Split deepblue/tools.py by domain~~ | ~~Agent architecture prep~~ | **DONE** |
| 2b | **DeepBlue comprehensive audit** | Performance, tool access, prompt quality, UX | Core product experience |
| 3 | Split agent_thread_service.py | Untangle 5 responsibilities | Email/thread agents |
| 4 | Extract business logic from fat routers | Clean architecture | All services |
| 5 | Extract large frontend pages | Maintainability | - |
| 6 | Standardize data fetching patterns | Reduce boilerplate | - |
